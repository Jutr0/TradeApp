import { Typography } from "@material-ui/core";
import { withRouter } from "react-router-dom";

const InventoryPage = () => {
  return <Typography variant="h2">InventoryPage</Typography>;
};

export default withRouter(InventoryPage);
