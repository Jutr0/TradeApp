import { Typography } from "@material-ui/core";
import { withRouter } from "react-router-dom";

const ShopPage = () => {
  return <Typography variant="h2">ShopPage</Typography>;
};

export default withRouter(ShopPage);
